
// função tradicional
function soma(a, b){
    return a + b;
}

// Arrow Function
const somaArrow = (a, b) => a + b;

// saida usando Template Literals `${soma(2,3)}`
console.log(`Função normal soma() : ${soma(2, 3)}`);
console.log(`Arrow Function  : ${soma(2, 3)}`);

